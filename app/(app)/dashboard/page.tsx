import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function DashboardPage() {
  return (
    <ScreenPlaceholder
      emoji="🏠"
      title="Dashboard"
      note="Próximo partido por cargar + mensajes personalizados del día."
    />
  );
}
