import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function TablaPage() {
  return (
    <ScreenPlaceholder
      emoji="🏆"
      title="Tabla de posiciones"
      note="Ranking del grupo por puntos, con plenos y aciertos como desempate."
    />
  );
}
