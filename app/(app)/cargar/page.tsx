import { ScreenPlaceholder } from "@/components/ui/ScreenPlaceholder";

export default function CargarPage() {
  return (
    <ScreenPlaceholder
      emoji="⚽"
      title="Cargar partido"
      note="Fixture por jugar: cargá o editá tu pronóstico hasta 1h antes del partido."
    />
  );
}
